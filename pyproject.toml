[project]
name = "comfyui-prompt-manager"
description = "Global prompt manager for ComfyUI - a floating toolbar injected into every multiline text widget, with SQLite storage, import/export and settings. No custom node required."
version = "1.1.0"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = []

[project.urls]
Repository = "https://github.com/live1617/comfyui-prompt-manager"

[tool.comfy]
PublisherId = "live1617"
DisplayName = "Prompt Manager (提示词管理器)"
Icon = ""
